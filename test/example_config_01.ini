[equation]
equation = D[u,{x,2}] = mu * nu * D[u,t]

# this section includes both numerical parameters and
# parameter names to find in the hdf5 data file
[parameters]
nu = 1.0
mu = MU

[spatial_coord_names]
x = X1
y = Y1

[spatial_coord_ordering]
spatial_coord_ordering = y, x

[temporal_coord_name]
t = T
