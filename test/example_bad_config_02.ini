[equation]
equatin = D[u,{x,2}] = mu * nu * D[u,t]

# this section includes both numerical parameters and
# parameter names to find in the hdf5 data file
[parameters]
nu = 1.0
mu = MU

[spatial_coord_names]
x = X1

[time_coord_name]
t = T
